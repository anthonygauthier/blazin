config:
  target: 'http://localhost:8000'
  phases:
    - duration: 180
      arrivalRate: 1
      maxVusers: 50
scenarios:
  - flow:
    - get:
        url: "/"
    - get:
        url: "/test"
    - get:
        url: "/test2"
